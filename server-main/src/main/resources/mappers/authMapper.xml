<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.enjoytrips.model.dao.AuthDao">
    <!-- 토큰 재발급-->
    <select id="refreshTokens" parameterType="map" resultType="token">
        SELECT
            access_token as accessToken,
            refresh_token as refreshToken
        FROM
            user
        WHERE
            user_id = #{id}

    </select>

    <!-- 회원 가입-->
    <insert id="regist" parameterType="user">
        INSERT INTO
            user(user_id, user_pw, user_name, user_admin, user_salt, user_email)
        VALUES
            (#{id}, #{pw}, #{name}, #{admin}, #{salt}, #{email})
    </insert>

    <!-- id 중복확인 -->
    <select id="isDuplicateId" parameterType="String" resultType="int">
        SELECT
            user_id
        FROM
            user
        WHERE
            user_id = #{id}
    </select>

    <!-- 로그인   -->
    <select id="login" parameterType="user">
        SELECT
            user_id, user_pw, user_name, user_admin, user_salt, user_email
        FROM
            user
        WHERE
            user_id = #{id} and user_pw = #{pw}
        ;
    </select>

</mapper>