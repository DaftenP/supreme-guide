<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.enjoytrips.model.dao.QnaCommentDao">
<!--       // 댓글 전체 조회
    List<QnaComment> list(int qnaId);
    // 댓글 수정
    int modify(QnaComment qnaComment);
    // 댓글 삭제
    int delete(int qnaCommentId);
    // 댓글 등록
    int regist(QnaComment qnaComment); -->

    <!--  댓글 전체 조회  -->
    <select id="list" parameterType="int" resultType="qnaComment">
        SELECT
            qna_comment_id, qna_comment_content, qna_comment_harmful, qna_qna_id, user_user_id
        FROM
            qna_comment
        WHERE
            qna_qna_id = #{qnaQnaId}
    </select>

    <!-- 댓글 등록 -->
    <insert id="regist" parameterType="qnaComment">
        INSERT INTO qna_comment(qna_comment_id, qna_comment_content, qna_comment_harmful, qna_qna_id, user_user_id)
        VALUES (#{qnaCommentId}, #{qnaCommentContent}, #{qnaCommentHarmful}, #{qnaQnaId}, #{userUserId})
    </insert>
    <!--  댓글 수정  -->
    <update id="modify" parameterType="qnaComment">
        UPDATE qna_comment
        SET qna_comment_content = #{qnaCommentContent}
        WHERE qna_comment_id = #{qnaCommentId}
    </update>

    <!-- 댓글 삭제-->
    <delete id="delete" parameterType="int">
        DELETE
        FROM qna_comment
        WHERE qna_comment_id = #{qnaCommentId}
    </delete>
</mapper>