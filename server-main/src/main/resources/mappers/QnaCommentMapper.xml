<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.enjoytrips.model.dao.QnaCommentDao">

    <!-- 댓글 작성자 조회   -->
    <select id="selectCommentWriter" resultType="com.ssafy.enjoytrips.model.dto.Comment">
        SELECT qna_comment_id, qna_id, user_id
        FROM qna_comment
        WHERE qna_comment_id = #{id}
    </select>

    <!-- 댓글 등록 -->
    <insert id="regist" parameterType="comment">
        INSERT INTO
            qna_comment(
                qna_comment_content,
                qna_comment_harmful,
                qna_id,
                user_id)
        VALUES (
            #{content},
            #{harmful},
            #{articleId},
            #{userId}
        )
        ;
    </insert>

    <!--  댓글 수정  -->
    <update id="modify" parameterType="comment">
        UPDATE
            qna_comment
        SET
            qna_comment_content = #{content},
            qna_comment_harmful = #{harmful}
        WHERE
            qna_comment_id = #{id}
    </update>

    <!-- 댓글 삭제-->
    <delete id="delete" parameterType="int">
        DELETE FROM
            qna_comment
        WHERE
            qna_comment_id = #{id}
    </delete>
</mapper>